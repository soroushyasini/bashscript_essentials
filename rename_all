# for example; to Rename all *.txt to *.text would be like this :
for file in *.txt; do 
mv -- "$file" "${file%.txt}.text"
done

###########
# I had some files with *.SAFE.zip extention and iu wanted to chage it to *.zip. so here is the code :

for file in *.SAFE.zip; do
mv -- "$file" "${file%.SAFE.zip}.zip";
done
